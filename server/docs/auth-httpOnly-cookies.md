# DEPRECATED: Authentication Documentation

This file is deprecated.

The backend now runs in **cookie-only authentication mode**. Header-based auth (`Authorization: Bearer` / `x-access-token`) is not supported.

Please use the canonical documentation:

- `server/docs/auth-cookie-only.md`
