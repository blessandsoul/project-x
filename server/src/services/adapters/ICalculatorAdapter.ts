/**
 * Calculator Adapter Interface
 * 
 * Re-exports the ICalculatorAdapter interface from types for consistency.
 * This file exists in the adapters directory for convenient imports.
 */
export type {
    ICalculatorAdapter,
    StandardCalculatorRequest,
    StandardCalculatorResponse,
    CalculatorConfig,
    CalculatorType,
    RequestFieldMapping,
    ResponseFieldMapping,
} from '../../types/calculatorAdapterTypes.js';
